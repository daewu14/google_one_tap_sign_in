name: google_one_tap_sign_in
description: Google One Tap Sign In (Android)
authors:
  - Daewu (daewu.bintara1996@gmail.com)
version: 0.0.1
homepage:

environment:
  sdk: ">=2.12.0 <3.0.0"
  flutter: ">=1.20.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^1.0.0

flutter:

  plugin:
    platforms:
      android:
        package: id.daewu14.google_one_tap_sign_in
        pluginClass: GoogleOneTapSignInPlugin
